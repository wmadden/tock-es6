#!/bin/bash

if [ "$1" = "-s" ]; then
  silent=true
  shift
fi

RED='\033[0;31m'
NC='\033[0m' # No Color

for command in $@; do
  if $silent; then
    echo -e "run ${RED}${command}${NC}";
    npm run -s $command;
  else
    npm run $command
  fi
done
